def separar_consoante(consoante: list):
    consoante = []
    nao_consoante = []

    for c in consoante:
        if c % 2 == 0:
            consoante.append(c)  
        else: 
            nao_consoante.append(c)



if __name__ == '__main__':
    separar_consoante = ()
    while True:
        separar_consoante.append(input(''))